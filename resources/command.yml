setpoint:

- setpoint

seepoint:

- seepoint

mypoint:

- mypoint

paypoint:

- paypoint

givepoint:

- givepoint

toppoint:

- toppoint

setlangpoint:

- setlangpoint

takepoint:

- takepoint

mystatuspoint:

- mystatuspoint
